class Cell{

  ArrayList elements;
  
  Cell() {
    elements = new ArrayList();
  }
  
  void step() {
    for( int i=0; i < elements.size(); i++ ) {
      
      Element ie = (Element) elements.get(i);
      for( int j=i  j < elments.size(); j++ ) {
        
        
      }
    }
  }
  
  void addElement(element) {
    elements.add(elment);
  }
}
